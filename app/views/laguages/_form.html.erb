<%= form_for(@laguage) do |f| %>
  <% if @laguage.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@laguage.errors.count, "error") %> prohibited this laguage from being saved:</h2>

      <ul>
      <% @laguage.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :version %><br>
    <%= f.text_field :version %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
